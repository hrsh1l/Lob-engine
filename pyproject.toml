[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lob-engine"
version = "0.1.0"
description = "A limit order book matching engine (price-time priority, stdlib only)"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["lob"]
